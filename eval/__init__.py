"""Capstone evaluation suite (demo).

Runs the pipeline headlessly over the golden data set (`eval.dataset`) and
scores each run on two things: golden-tier accuracy and the structural quality
of the generated test cases. See `eval.run_evals` for the CLI.

Entrypoint: ``python -m eval.run_evals``
"""
